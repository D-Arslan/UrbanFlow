[pytest]
# On limite la découverte au dossier tests/ : les scripts des autres dossiers (ml/, consumer/…)
# ne sont pas des tests pytest et importent des deps lourdes (kafka, torch...) absentes de la CI.
testpaths = tests
