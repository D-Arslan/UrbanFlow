[pytest]
# On limite la découverte au dossier tests/ : les scripts Sprint 1-3 nommés `test_*`
# (ex. consumer/test_consumer.py) ne sont PAS des tests pytest et importent des deps
# lourdes (kafka, torch...) absentes de l'environnement de CI léger.
testpaths = tests
